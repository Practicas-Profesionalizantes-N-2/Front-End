import { StyleSheet } from 'react-native'

//estilos de los inputs
export const InputStyles = StyleSheet.create({
    input: {
        height: 40,
        margin: 12,
        borderWidth: 1,
        padding: 10,
        borderColor: '#5F4866',
        borderRadius: 8 
    },
});