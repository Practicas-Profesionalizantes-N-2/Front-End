import { StyleSheet } from 'react-native'

//estilos de los inputs
export const InputStyles = StyleSheet.create({
    input: {
        height: 50,
        margin: 1,
        borderWidth: 1,
        padding: 10,
        borderColor: '#5F4866',
        borderRadius: 8,
        marginTop:40,
        marginBottom:10
    },
});