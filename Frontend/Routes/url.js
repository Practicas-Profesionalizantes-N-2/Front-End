 // Direccion la brinda amazon y la cambian cada vez que arranca el server

const ip = '3.135.194.105'

export const URLregister = `http://${ip}:3000/api/register`
export const URLlogin = `http://${ip}:3000/api/login`