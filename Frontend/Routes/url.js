 // Direccion la brinda amazon y la cambian cada vez que arranca el server

const url = '3.144.83.220'

export const URLregister = `http://${url}:3000/api/register`
export const URLlogin = `http://${url}:3000/api/login`